plugins {
    id "nf-duckdbcache@${System.getenv("PLUGIN_VERSION") ?: "latest"}"
}

duckdbcache{
    postgre{
        enabled = true
        host = "127.0.0.1"
        user = "user1"
        password = "password1"
        database = "postgres"
    }
}